import * as Parameters from './parameters';
import { Client } from '../clients';
import { Callback } from '../types';
export declare class Batch {
    private client;
    constructor(client: Client);
    /** Make up to 10 GET requests in a single, batched API call. */
    getBatch<T = unknown>(parameters: Parameters.GetBatch, callback: Callback<T>): Promise<void>;
    /** Make up to 10 GET requests in a single, batched API call. */
    getBatch<T = unknown>(parameters: Parameters.GetBatch, callback?: never): Promise<T>;
}
